# Database Configuration
DB_PASSWORD=your_db_password_here
DB_USER=postgres
DB_NAME=farmatodo_db
DB_PORT=5432

# Application Security
# IMPORTANT: Use a strong random key (minimum 32 characters) in production
ENCRYPTION_KEY=change_this_to_a_secure_random_key_minimum_32_characters
API_KEY=change_this_to_a_secure_api_key

# SMTP Configuration (MailHog for local development)
SPRING_MAIL_HOST=mailhog
SPRING_MAIL_PORT=1025

# Application Port
APP_PORT=8080

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with actual credentials
# 3. NEVER commit .env to version control
